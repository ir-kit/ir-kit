// AUTO-GENERATED by @ahmedrowaihi/k6-gen — do not edit by hand.

import { applyMiddlewareHeaders } from "@ahmedrowaihi/k6/runtime";
import * as http from "k6/http";
import type {
  ApiResponse,
  Category,
  Circle,
  KindEnum,
  KindEnum2,
  Order,
  Pet,
  Rect,
  SchemaEnum,
  StatusEnum,
  Tag,
  User,
} from "./types.js";

declare const __ENV: Record<string, string | undefined>;

export const BASE_URL: string =
  (typeof __ENV !== "undefined" && __ENV.BASE_URL) ||
  "https://petstore3.swagger.io/api/v3";

export interface CallOpts {
  headers?: Record<string, string>;
  tags?: Record<string, string>;
}

function __buildQuery(p: Record<string, unknown>): string {
  const parts: string[] = [];
  for (const [k, v] of Object.entries(p)) {
    if (v === undefined || v === null) continue;
    const key = encodeURIComponent(k);
    if (Array.isArray(v)) {
      for (const item of v)
        parts.push(`${key}=${encodeURIComponent(String(item))}`);
    } else {
      parts.push(`${key}=${encodeURIComponent(String(v))}`);
    }
  }
  return parts.length ? "?" + parts.join("&") : "";
}

function __parseJson(res: http.Response): unknown {
  return res.body ? JSON.parse(res.body as string) : undefined;
}

function __mergeTags(
  op: string,
  extra: Record<string, string> | undefined,
): Record<string, string> {
  return { operation: op, ...(extra ?? {}) };
}

export function addPet(body: Pet, opts?: CallOpts): Pet {
  const url = BASE_URL + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("addPet", opts?.tags),
    headers,
  });
  return __parseJson(res) as Pet;
}

export function updatePet(body: Pet, opts?: CallOpts): Pet {
  const url = BASE_URL + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.put(url, JSON.stringify(body), {
    tags: __mergeTags("updatePet", opts?.tags),
    headers,
  });
  return __parseJson(res) as Pet;
}

export function findPetsByStatus(
  query: {
    status: SchemaEnum;
  },
  opts?: CallOpts,
): Array<Pet> {
  const url =
    BASE_URL +
    "/pet/findByStatus" +
    __buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("findPetsByStatus", opts?.tags),
    headers,
  });
  return __parseJson(res) as Array<Pet>;
}

export function findPetsByTags(
  query: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): Array<Pet> {
  const url =
    BASE_URL +
    "/pet/findByTags" +
    __buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("findPetsByTags", opts?.tags),
    headers,
  });
  return __parseJson(res) as Array<Pet>;
}

export function getPetById(petId: number, opts?: CallOpts): Pet {
  const url = BASE_URL + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("getPetById", opts?.tags),
    headers,
  });
  return __parseJson(res) as Pet;
}

export function updatePetWithForm(
  petId: number,
  query?: {
    name?: string;
    status?: string;
  },
  opts?: CallOpts,
): Pet {
  const url =
    BASE_URL + "/pet/" + petId + __buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.post(url, null, {
    tags: __mergeTags("updatePetWithForm", opts?.tags),
    headers,
  });
  return __parseJson(res) as Pet;
}

export function deletePet(petId: number, opts?: CallOpts): unknown {
  const url = BASE_URL + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.del(url, null, {
    tags: __mergeTags("deletePet", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}

export function uploadFile(
  petId: number,
  query?: {
    additionalMetadata?: string;
  },
  body?: string,
  opts?: CallOpts,
): ApiResponse {
  const url =
    BASE_URL +
    "/pet/" +
    petId +
    "/uploadImage" +
    __buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/octet-stream",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("uploadFile", opts?.tags),
    headers,
  });
  return __parseJson(res) as ApiResponse;
}

export function uploadPetDocument(
  petId: number,
  body: {
    file: string;
    title?: string;
    description?: string;
  },
  opts?: CallOpts,
): ApiResponse {
  const url = BASE_URL + "/pet/" + petId + "/uploadDocument";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "multipart/form-data",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("uploadPetDocument", opts?.tags),
    headers,
  });
  return __parseJson(res) as ApiResponse;
}

export function getInventory(opts?: CallOpts): Record<string, unknown> {
  const url = BASE_URL + "/store/inventory";
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("getInventory", opts?.tags),
    headers,
  });
  return __parseJson(res) as Record<string, unknown>;
}

export function placeOrder(body?: Order, opts?: CallOpts): Order {
  const url = BASE_URL + "/store/order";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("placeOrder", opts?.tags),
    headers,
  });
  return __parseJson(res) as Order;
}

export function getOrderById(orderId: number, opts?: CallOpts): Order {
  const url = BASE_URL + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("getOrderById", opts?.tags),
    headers,
  });
  return __parseJson(res) as Order;
}

export function deleteOrder(orderId: number, opts?: CallOpts): unknown {
  const url = BASE_URL + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.del(url, null, {
    tags: __mergeTags("deleteOrder", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}

export function createUser(body?: User, opts?: CallOpts): User {
  const url = BASE_URL + "/user";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("createUser", opts?.tags),
    headers,
  });
  return __parseJson(res) as User;
}

export function createUsersWithListInput(
  body?: Array<User>,
  opts?: CallOpts,
): User {
  const url = BASE_URL + "/user/createWithList";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("createUsersWithListInput", opts?.tags),
    headers,
  });
  return __parseJson(res) as User;
}

export function loginUser(
  query?: {
    username?: string;
    password?: string;
  },
  opts?: CallOpts,
): string {
  const url =
    BASE_URL + "/user/login" + __buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("loginUser", opts?.tags),
    headers,
  });
  return __parseJson(res) as string;
}

export function logoutUser(opts?: CallOpts): unknown {
  const url = BASE_URL + "/user/logout";
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("logoutUser", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}

export function getUserByName(username: string, opts?: CallOpts): User {
  const url = BASE_URL + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.get(url, {
    tags: __mergeTags("getUserByName", opts?.tags),
    headers,
  });
  return __parseJson(res) as User;
}

export function updateUser(
  username: string,
  body?: User,
  opts?: CallOpts,
): unknown {
  const url = BASE_URL + "/user/" + username;
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.put(url, JSON.stringify(body), {
    tags: __mergeTags("updateUser", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}

export function deleteUser(username: string, opts?: CallOpts): unknown {
  const url = BASE_URL + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...(opts?.headers || {}) });
  const res = http.del(url, null, {
    tags: __mergeTags("deleteUser", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}

export function submitTags(
  body: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): {
  count?: number;
} {
  const url = BASE_URL + "/tags";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("submitTags", opts?.tags),
    headers,
  });
  return __parseJson(res) as {
    count?: number;
  };
}

export function updateProfile(
  body: {
    name: string;
    nickname?: string | null;
  },
  opts?: CallOpts,
): {
  ok?: boolean;
} {
  const url = BASE_URL + "/profile";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("updateProfile", opts?.tags),
    headers,
  });
  return __parseJson(res) as {
    ok?: boolean;
  };
}

export function createShape(
  body:
    | ({
        kind: string;
      } & Circle)
    | ({
        kind: string;
      } & Rect),
  opts?: CallOpts,
): {
  id?: string;
} {
  const url = BASE_URL + "/shapes";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("createShape", opts?.tags),
    headers,
  });
  return __parseJson(res) as {
    id?: string;
  };
}

export function submitMeasurement(
  body: {
    value: number;
  },
  opts?: CallOpts,
): unknown {
  const url = BASE_URL + "/measurements";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...(opts?.headers || {}),
  });
  const res = http.post(url, JSON.stringify(body), {
    tags: __mergeTags("submitMeasurement", opts?.tags),
    headers,
  });
  return __parseJson(res) as unknown;
}
