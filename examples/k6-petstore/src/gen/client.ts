// AUTO-GENERATED by @ahmedrowaihi/k6-gen — do not edit by hand.

import {
  applyMiddlewareHeaders,
  applyMiddlewareParams,
  buildQuery,
  getBaseUrl,
  installK6Bridge,
  installMetricsFactory,
  mergeTags,
  parseJson,
  setExecModule,
} from "@ahmedrowaihi/k6/runtime";

import { check, group, sleep } from "k6";
import exec from "k6/execution";
import * as http from "k6/http";
import { Counter, Gauge, Rate, Trend } from "k6/metrics";

import type * as T from "./types.js";

installK6Bridge({ check, group, sleep });

setExecModule(exec);

installMetricsFactory(
  (kind: "counter" | "gauge" | "rate" | "trend", name: string) =>
    new ({ counter: Counter, gauge: Gauge, rate: Rate, trend: Trend })[kind](
      name,
    ),
);

const DEFAULT_BASE_URL: string = "https://petstore3.swagger.io/api/v3";

export type CallOpts = {
  headers?: Record<string, string>;
  tags?: Record<string, string>;
  timeout?: string | number;
  redirects?: number;
  compression?: string;
  responseType?: "text" | "binary" | "none";
};

export function addPet(body: T.Pet, opts?: CallOpts): T.Pet {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("addPet", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.Pet;
}

export function updatePet(body: T.Pet, opts?: CallOpts): T.Pet {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updatePet", opts?.tags),
  };
  const res = http.put(url, JSON.stringify(body), params);
  return parseJson(res) as T.Pet;
}

export function findPetsByStatus(
  query: {
    status: T.SchemaEnum;
  },
  opts?: CallOpts,
): Array<T.Pet> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByStatus" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("findPetsByStatus", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as Array<T.Pet>;
}

export function findPetsByTags(
  query: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): Array<T.Pet> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByTags" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("findPetsByTags", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as Array<T.Pet>;
}

export function getPetById(petId: number, opts?: CallOpts): T.Pet {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getPetById", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as T.Pet;
}

export function updatePetWithForm(
  petId: number,
  query?: {
    name?: string;
    status?: string;
  },
  opts?: CallOpts,
): T.Pet {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updatePetWithForm", opts?.tags),
  };
  const res = http.post(url, null, params);
  return parseJson(res) as T.Pet;
}

export function deletePet(petId: number, opts?: CallOpts): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deletePet", opts?.tags),
  };
  const res = http.del(url, null, params);
  return parseJson(res) as unknown;
}

export function uploadFile(
  petId: number,
  query?: {
    additionalMetadata?: string;
  },
  body?: string,
  opts?: CallOpts,
): T.ApiResponse {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    "/uploadImage" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/octet-stream",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("uploadFile", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.ApiResponse;
}

export function uploadPetDocument(
  petId: number,
  body: {
    file: string;
    title?: string;
    description?: string;
  },
  opts?: CallOpts,
): T.ApiResponse {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId + "/uploadDocument";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "multipart/form-data",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("uploadPetDocument", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.ApiResponse;
}

export function getInventory(opts?: CallOpts): Record<string, unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/inventory";
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getInventory", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as Record<string, unknown>;
}

export function placeOrder(body?: T.Order, opts?: CallOpts): T.Order {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("placeOrder", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.Order;
}

export function getOrderById(orderId: number, opts?: CallOpts): T.Order {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getOrderById", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as T.Order;
}

export function deleteOrder(orderId: number, opts?: CallOpts): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deleteOrder", opts?.tags),
  };
  const res = http.del(url, null, params);
  return parseJson(res) as unknown;
}

export function createUser(body?: T.User, opts?: CallOpts): T.User {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createUser", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.User;
}

export function createUsersWithListInput(
  body?: Array<T.User>,
  opts?: CallOpts,
): T.User {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/createWithList";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createUsersWithListInput", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as T.User;
}

export function loginUser(
  query?: {
    username?: string;
    password?: string;
  },
  opts?: CallOpts,
): string {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/user/login" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("loginUser", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as string;
}

export function logoutUser(opts?: CallOpts): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/logout";
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("logoutUser", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as unknown;
}

export function getUserByName(username: string, opts?: CallOpts): T.User {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getUserByName", opts?.tags),
  };
  const res = http.get(url, params);
  return parseJson(res) as T.User;
}

export function updateUser(
  username: string,
  body?: T.User,
  opts?: CallOpts,
): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updateUser", opts?.tags),
  };
  const res = http.put(url, JSON.stringify(body), params);
  return parseJson(res) as unknown;
}

export function deleteUser(username: string, opts?: CallOpts): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deleteUser", opts?.tags),
  };
  const res = http.del(url, null, params);
  return parseJson(res) as unknown;
}

export function submitTags(
  body: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): {
  count?: number;
} {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/tags";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("submitTags", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as {
    count?: number;
  };
}

export function updateProfile(
  body: {
    name: string;
    nickname?: string | null;
  },
  opts?: CallOpts,
): {
  ok?: boolean;
} {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/profile";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updateProfile", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as {
    ok?: boolean;
  };
}

export function createShape(
  body:
    | ({
        kind: "circle";
      } & T.Circle)
    | ({
        kind: "rect";
      } & T.Rect),
  opts?: CallOpts,
): {
  id?: string;
} {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/shapes";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createShape", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as {
    id?: string;
  };
}

export function submitMeasurement(
  body: {
    value: number;
  },
  opts?: CallOpts,
): unknown {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/measurements";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("submitMeasurement", opts?.tags),
  };
  const res = http.post(url, JSON.stringify(body), params);
  return parseJson(res) as unknown;
}

async function addPet_async(body: T.Pet, opts?: CallOpts): Promise<T.Pet> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("addPet", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.Pet;
}

async function updatePet_async(body: T.Pet, opts?: CallOpts): Promise<T.Pet> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updatePet", opts?.tags),
  };
  const res = await http.asyncRequest("PUT", url, JSON.stringify(body), params);
  return parseJson(res) as T.Pet;
}

async function findPetsByStatus_async(
  query: {
    status: T.SchemaEnum;
  },
  opts?: CallOpts,
): Promise<Array<T.Pet>> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByStatus" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("findPetsByStatus", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as Array<T.Pet>;
}

async function findPetsByTags_async(
  query: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): Promise<Array<T.Pet>> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByTags" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("findPetsByTags", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as Array<T.Pet>;
}

async function getPetById_async(
  petId: number,
  opts?: CallOpts,
): Promise<T.Pet> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getPetById", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as T.Pet;
}

async function updatePetWithForm_async(
  petId: number,
  query?: {
    name?: string;
    status?: string;
  },
  opts?: CallOpts,
): Promise<T.Pet> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updatePetWithForm", opts?.tags),
  };
  const res = await http.asyncRequest("POST", url, null, params);
  return parseJson(res) as T.Pet;
}

async function deletePet_async(
  petId: number,
  opts?: CallOpts,
): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deletePet", opts?.tags),
  };
  const res = await http.asyncRequest("DELETE", url, null, params);
  return parseJson(res) as unknown;
}

async function uploadFile_async(
  petId: number,
  query?: {
    additionalMetadata?: string;
  },
  body?: string,
  opts?: CallOpts,
): Promise<T.ApiResponse> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    "/uploadImage" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/octet-stream",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("uploadFile", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.ApiResponse;
}

async function uploadPetDocument_async(
  petId: number,
  body: {
    file: string;
    title?: string;
    description?: string;
  },
  opts?: CallOpts,
): Promise<T.ApiResponse> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId + "/uploadDocument";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "multipart/form-data",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("uploadPetDocument", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.ApiResponse;
}

async function getInventory_async(
  opts?: CallOpts,
): Promise<Record<string, unknown>> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/inventory";
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getInventory", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as Record<string, unknown>;
}

async function placeOrder_async(
  body?: T.Order,
  opts?: CallOpts,
): Promise<T.Order> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("placeOrder", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.Order;
}

async function getOrderById_async(
  orderId: number,
  opts?: CallOpts,
): Promise<T.Order> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getOrderById", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as T.Order;
}

async function deleteOrder_async(
  orderId: number,
  opts?: CallOpts,
): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deleteOrder", opts?.tags),
  };
  const res = await http.asyncRequest("DELETE", url, null, params);
  return parseJson(res) as unknown;
}

async function createUser_async(
  body?: T.User,
  opts?: CallOpts,
): Promise<T.User> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createUser", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.User;
}

async function createUsersWithListInput_async(
  body?: Array<T.User>,
  opts?: CallOpts,
): Promise<T.User> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/createWithList";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createUsersWithListInput", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as T.User;
}

async function loginUser_async(
  query?: {
    username?: string;
    password?: string;
  },
  opts?: CallOpts,
): Promise<string> {
  const url =
    getBaseUrl(DEFAULT_BASE_URL) +
    "/user/login" +
    buildQuery(query as Record<string, unknown>);
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("loginUser", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as string;
}

async function logoutUser_async(opts?: CallOpts): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/logout";
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("logoutUser", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as unknown;
}

async function getUserByName_async(
  username: string,
  opts?: CallOpts,
): Promise<T.User> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("getUserByName", opts?.tags),
  };
  const res = await http.asyncRequest("GET", url, null, params);
  return parseJson(res) as T.User;
}

async function updateUser_async(
  username: string,
  body?: T.User,
  opts?: CallOpts,
): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updateUser", opts?.tags),
  };
  const res = await http.asyncRequest("PUT", url, JSON.stringify(body), params);
  return parseJson(res) as unknown;
}

async function deleteUser_async(
  username: string,
  opts?: CallOpts,
): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username;
  const headers = applyMiddlewareHeaders({ ...opts?.headers });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("deleteUser", opts?.tags),
  };
  const res = await http.asyncRequest("DELETE", url, null, params);
  return parseJson(res) as unknown;
}

async function submitTags_async(
  body: {
    tags: Array<string>;
  },
  opts?: CallOpts,
): Promise<{
  count?: number;
}> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/tags";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("submitTags", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as {
    count?: number;
  };
}

async function updateProfile_async(
  body: {
    name: string;
    nickname?: string | null;
  },
  opts?: CallOpts,
): Promise<{
  ok?: boolean;
}> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/profile";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("updateProfile", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as {
    ok?: boolean;
  };
}

async function createShape_async(
  body:
    | ({
        kind: "circle";
      } & T.Circle)
    | ({
        kind: "rect";
      } & T.Rect),
  opts?: CallOpts,
): Promise<{
  id?: string;
}> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/shapes";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("createShape", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as {
    id?: string;
  };
}

async function submitMeasurement_async(
  body: {
    value: number;
  },
  opts?: CallOpts,
): Promise<unknown> {
  const url = getBaseUrl(DEFAULT_BASE_URL) + "/measurements";
  const headers = applyMiddlewareHeaders({
    "Content-Type": "application/json",
    ...opts?.headers,
  });
  const params = {
    ...applyMiddlewareParams(),
    ...opts,
    headers,
    tags: mergeTags("submitMeasurement", opts?.tags),
  };
  const res = await http.asyncRequest(
    "POST",
    url,
    JSON.stringify(body),
    params,
  );
  return parseJson(res) as unknown;
}

addPet.spec = (body: T.Pet, opts?: CallOpts) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/pet",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("addPet", opts?.tags),
  },
});

updatePet.spec = (body: T.Pet, opts?: CallOpts) => ({
  method: "PUT",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/pet",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("updatePet", opts?.tags),
  },
});

findPetsByStatus.spec = (
  query: {
    status: T.SchemaEnum;
  },
  opts?: CallOpts,
) => ({
  method: "GET",
  url:
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByStatus" +
    buildQuery(query as Record<string, unknown>),
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("findPetsByStatus", opts?.tags),
  },
});

findPetsByTags.spec = (
  query: {
    tags: Array<string>;
  },
  opts?: CallOpts,
) => ({
  method: "GET",
  url:
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/findByTags" +
    buildQuery(query as Record<string, unknown>),
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("findPetsByTags", opts?.tags),
  },
});

getPetById.spec = (petId: number, opts?: CallOpts) => ({
  method: "GET",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("getPetById", opts?.tags),
  },
});

updatePetWithForm.spec = (
  petId: number,
  query?: {
    name?: string;
    status?: string;
  },
  opts?: CallOpts,
) => ({
  method: "POST",
  url:
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    buildQuery(query as Record<string, unknown>),
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("updatePetWithForm", opts?.tags),
  },
});

deletePet.spec = (petId: number, opts?: CallOpts) => ({
  method: "DELETE",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("deletePet", opts?.tags),
  },
});

uploadFile.spec = (
  petId: number,
  query?: {
    additionalMetadata?: string;
  },
  body?: string,
  opts?: CallOpts,
) => ({
  method: "POST",
  url:
    getBaseUrl(DEFAULT_BASE_URL) +
    "/pet/" +
    petId +
    "/uploadImage" +
    buildQuery(query as Record<string, unknown>),
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/octet-stream",
      ...opts?.headers,
    }),
    tags: mergeTags("uploadFile", opts?.tags),
  },
});

uploadPetDocument.spec = (
  petId: number,
  body: {
    file: string;
    title?: string;
    description?: string;
  },
  opts?: CallOpts,
) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/pet/" + petId + "/uploadDocument",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "multipart/form-data",
      ...opts?.headers,
    }),
    tags: mergeTags("uploadPetDocument", opts?.tags),
  },
});

getInventory.spec = (opts?: CallOpts) => ({
  method: "GET",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/store/inventory",
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("getInventory", opts?.tags),
  },
});

placeOrder.spec = (body?: T.Order, opts?: CallOpts) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/store/order",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("placeOrder", opts?.tags),
  },
});

getOrderById.spec = (orderId: number, opts?: CallOpts) => ({
  method: "GET",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("getOrderById", opts?.tags),
  },
});

deleteOrder.spec = (orderId: number, opts?: CallOpts) => ({
  method: "DELETE",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/store/order/" + orderId,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("deleteOrder", opts?.tags),
  },
});

createUser.spec = (body?: T.User, opts?: CallOpts) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("createUser", opts?.tags),
  },
});

createUsersWithListInput.spec = (body?: Array<T.User>, opts?: CallOpts) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user/createWithList",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("createUsersWithListInput", opts?.tags),
  },
});

loginUser.spec = (
  query?: {
    username?: string;
    password?: string;
  },
  opts?: CallOpts,
) => ({
  method: "GET",
  url:
    getBaseUrl(DEFAULT_BASE_URL) +
    "/user/login" +
    buildQuery(query as Record<string, unknown>),
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("loginUser", opts?.tags),
  },
});

logoutUser.spec = (opts?: CallOpts) => ({
  method: "GET",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user/logout",
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("logoutUser", opts?.tags),
  },
});

getUserByName.spec = (username: string, opts?: CallOpts) => ({
  method: "GET",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("getUserByName", opts?.tags),
  },
});

updateUser.spec = (username: string, body?: T.User, opts?: CallOpts) => ({
  method: "PUT",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username,
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("updateUser", opts?.tags),
  },
});

deleteUser.spec = (username: string, opts?: CallOpts) => ({
  method: "DELETE",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/user/" + username,
  body: null,
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({ ...opts?.headers }),
    tags: mergeTags("deleteUser", opts?.tags),
  },
});

submitTags.spec = (
  body: {
    tags: Array<string>;
  },
  opts?: CallOpts,
) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/tags",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("submitTags", opts?.tags),
  },
});

updateProfile.spec = (
  body: {
    name: string;
    nickname?: string | null;
  },
  opts?: CallOpts,
) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/profile",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("updateProfile", opts?.tags),
  },
});

createShape.spec = (
  body:
    | ({
        kind: "circle";
      } & T.Circle)
    | ({
        kind: "rect";
      } & T.Rect),
  opts?: CallOpts,
) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/shapes",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("createShape", opts?.tags),
  },
});

submitMeasurement.spec = (
  body: {
    value: number;
  },
  opts?: CallOpts,
) => ({
  method: "POST",
  url: getBaseUrl(DEFAULT_BASE_URL) + "/measurements",
  body: JSON.stringify(body),
  params: {
    ...applyMiddlewareParams(),
    ...opts,
    headers: applyMiddlewareHeaders({
      "Content-Type": "application/json",
      ...opts?.headers,
    }),
    tags: mergeTags("submitMeasurement", opts?.tags),
  },
});

export const async = {
  addPet: addPet_async,
  updatePet: updatePet_async,
  findPetsByStatus: findPetsByStatus_async,
  findPetsByTags: findPetsByTags_async,
  getPetById: getPetById_async,
  updatePetWithForm: updatePetWithForm_async,
  deletePet: deletePet_async,
  uploadFile: uploadFile_async,
  uploadPetDocument: uploadPetDocument_async,
  getInventory: getInventory_async,
  placeOrder: placeOrder_async,
  getOrderById: getOrderById_async,
  deleteOrder: deleteOrder_async,
  createUser: createUser_async,
  createUsersWithListInput: createUsersWithListInput_async,
  loginUser: loginUser_async,
  logoutUser: logoutUser_async,
  getUserByName: getUserByName_async,
  updateUser: updateUser_async,
  deleteUser: deleteUser_async,
  submitTags: submitTags_async,
  updateProfile: updateProfile_async,
  createShape: createShape_async,
  submitMeasurement: submitMeasurement_async,
};
