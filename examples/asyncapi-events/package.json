{
  "name": "@ir-kit/example-asyncapi-events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end AsyncAPI playground: `pnpm gen` emits the SDK from fixtures/user-events.yaml via @ir-kit/asyncapi-typescript; producer/consumer demos drive it against a local RabbitMQ broker.",
  "scripts": {
    "gen": "jiti gen.ts",
    "broker:up": "docker compose up -d",
    "broker:down": "docker compose down",
    "producer": "jiti src/producer.ts",
    "consumer": "jiti src/consumer.ts"
  },
  "dependencies": {
    "amqplib": "^0.10.4"
  },
  "devDependencies": {
    "@ir-kit/asyncapi-typescript": "workspace:*",
    "@ir-kit/tsconfig": "workspace:*",
    "@types/amqplib": "^0.10.5",
    "@types/node": "catalog:",
    "jiti": "^2.6.1"
  }
}
